meta {
  name: Update Content
  type: http
  seq: 6
}

put {
  url: {{baseUrl}}/content/{{contentId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "title": "Intro to ML — Updated",
    "difficulty": "intermediate",
    "duration_minutes": 60
  }
}

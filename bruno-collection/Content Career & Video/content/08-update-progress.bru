meta {
  name: Update Progress
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/content/{{contentId}}/progress
  body: json
  auth: bearer
}

auth:bearer {
  token: {{studentToken}}
}

body:json {
  {
    "progress_percent": 60,
    "status": "in_progress"
  }
}
